apiVersion: apps.kristian.io/v1alpha1
kind: KristianApp
metadata:
  name: myapp
  namespace: myapp-dev
spec:
  image: gcr.io/myproject/myapp:latest
  environment: dev
  autoscaling:
    enable: true
  ingress:
    enable: false
