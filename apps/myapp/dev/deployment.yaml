apiVersion: apps.kristian.io/v1alpha1
kind: KristianApp
metadata:
  name: myapp
  namespace: myapp-dev
spec:
  image: gcr.io/google-samples/hello-app:1.0
  environment: dev
  autoscaling:
    enable: true
  ingress:
    enable: false
