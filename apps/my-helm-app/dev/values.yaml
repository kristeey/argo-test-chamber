# THESE VALUES ARE MANAGED BY PLATFORM TEAM
# DO NOT EDIT THIS LINE MANUALLY
argocd:
  HELM_VERSION: 0.0.8
  AUTO_IMAGE_UPDATE_ENABLED: "true"
##################################################
# Edit values below to fit your environment      #
# For all configurable options, see chart        #
# 
##################################################
team: "test-team"
product: "test-product"
environment: dev
image:
  name: "tmpplatformjokesacr-cwbddta9eveuc0gu.azurecr.io/platform-jokes-frontend"
  tag: "dev"
  pullSecrets:
  - name: acr-secret
ingress:
  enabled: false
resources:
  memory: 256Mi
autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 2
podDisruptionBudget:
  enabled: false
serviceAccount:
  create: true
# Disable probes for CI - the hello-app doesn't have a /health endpoint
containers:
  startupProbe:
    enabled: false
  readinessProbe:
    enabled: false
  livenessProbe:
    enabled: false
