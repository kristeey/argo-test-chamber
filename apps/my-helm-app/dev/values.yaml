# THESE VALUES ARE MANAGED BY PLATFORM TEAM
# DO NOT EDIT THIS LINE MANUALLY
HELM_VERSION: 0.0.4
AUTO_IMAGE_UPDATE_ENABLED: "true"
AUTO_IMAGE_UPDATE_TAG: dev

##################################################
# Edit values below to fit your environment      #
# For all configurable options, see chart        #
# 
##################################################

team: "test-team"
product: "test-product"
environment: dev

image:
  repo: "tmpplatformjokesacr-cwbddta9eveuc0gu.azurecr.io/platform-jokes-api"
  tag: "dev"

ingress:
  enabled: false

resources:
  memory: 64Mi

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 2

podDisruptionBudget:
  enabled: false

serviceAccount:
  create: true

# Disable probes for CI - the hello-app doesn't have a /health endpoint
containers:
  startupProbe:
    enabled: false
  readinessProbe:
    enabled: false
  livenessProbe:
    enabled: false